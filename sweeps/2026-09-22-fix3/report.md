# fix3 — Fix A + Fix B (no Fix C)

Variant: war armyTarget scaling + farRinged muster-engage; demobilize logic
untouched.

Result: 140/215 still capped; control wins 66/152/172 hold. Established that
the garrison-march fix alone does not flip the targets — the demobilize
behavior is implicated. Superseded by fix4 (adds Fix C).
