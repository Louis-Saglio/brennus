# British-specific buildings of 0 A.D. 0.28.0

One file per building that **only the britons** can build (single-civ structures of the `generic/buildings/` analysis). Stats are the fully resolved brit templates; the shared buildings are documented in `docs/game_description/generic/buildings/`.

## Index

| Name | Type |
|---|---|
| [crannog](crannog) | building |
| [kennel](kennel) | building |

Vestigial brit templates, not buildable by anything (no `Builder` list
references them): the rotary mill (`structures/brit/rotarymill`) and the
archery range (`structures/brit/range`) — see `../civ.md`.
